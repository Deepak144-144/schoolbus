# Database
DATABASE_URL=""

# Supabase (client-safe keys only)
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""

# Supabase (server-only - NEVER expose to client)
SUPABASE_SERVICE_ROLE_KEY=""

# Auth
JWT_SECRET=""
JWT_EXPIRES_IN="7d"

# URLs
NEXTAUTH_URL=""
CORS_ORIGIN=""

# Demo mode (set to false in production)
DEMO_MODE="false"

# Demo passwords (only used when DEMO_MODE=true)
ADMIN_PASSWORD=""
DRIVER_PASSWORD=""
PARENT_PASSWORD=""

# GPS
GPS_UPDATE_INTERVAL="8000"